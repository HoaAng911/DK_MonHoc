<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Danh sách môn học</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        h1 {
            text-align: center;
            color: #333;
            margin-bottom: 30px;
            font-weight: bold;
            font-size: 2rem;
        }

        /* Tùy chỉnh bảng */
        .table-container {
            max-width: 1200px;
            margin: 0 auto;
            background-color: #fff;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            overflow: hidden;
        }

        .table {
            margin-bottom: 0;
        }

        .table th {
            background-color: #4CAF50;
            color: white;
            font-weight: bold;
            text-align: center;
            padding: 15px;
            border: none;
        }

        .table td {
            text-align: center;
            padding: 15px;
            vertical-align: middle;
            border: 1px solid #ddd;
        }

        .table tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        .table tr:hover {
            background-color: #f1f1f1;
            transition: background-color 0.3s ease;
        }

        /* Tùy chỉnh nút */
        .btn-action {
            text-decoration: none;
            color: #fff;
            padding: 6px 12px;
            border-radius: 25px;
            transition: background-color 0.3s ease, transform 0.2s ease;
            font-size: 0.9rem;
            display: inline-flex;
            align-items: center;
        }

        .btn-register {
            background-color: #007BFF;
        }

        .btn-register:hover {
            background-color: #0056b3;
            transform: translateY(-2px);
        }

        .btn-waiting {
            background-color: #ff9800;
        }

        .btn-waiting:hover {
            background-color: #e68900;
            transform: translateY(-2px);
        }

        /* Tùy chỉnh nút Xem danh sách đăng ký */
        .btn-enrolled {
            background-color: #28a745;
            /* Màu xanh lá để phân biệt với các nút khác */
        }

        .btn-enrolled:hover {
            background-color: #218838;
            transform: translateY(-2px);
        }

        /* Tùy chỉnh nút Quay về trang chủ */
        .btn-home {
            background-color: #6c757d;
            /* Màu xám để phân biệt với các nút khác */
        }

        .btn-home:hover {
            background-color: #5a6268;
            transform: translateY(-2px);
        }

        /* Đảm bảo bảng responsive */
        @media (max-width: 768px) {
            h1 {
                font-size: 1.5rem;
            }

            .table-container {
                margin: 10px;
            }

            .table th,
            .table td {
                padding: 10px;
                font-size: 0.85rem;
            }

            .btn-action {
                padding: 4px 8px;
                font-size: 0.8rem;
            }
        }

        /* Footer đơn giản */
        footer {
            background: linear-gradient(90deg, #007bff, #00c4cc);
            color: white;
            text-align: center;
            padding: 1rem 0;
            margin-top: auto;
        }

        footer p {
            margin: 0;
            font-size: 0.9rem;
        }

        /* Container cho nút Quay về trang chủ */
        .button-container {
            text-align: center;
            margin-top: 20px;
        }
    </style>
</head>

<body>
    <!-- Tiêu đề -->
    <h1><i class="fas fa-book-open me-2"></i>Các môn học hiện có</h1>

    <!-- Bảng danh sách môn học -->
    <div class="table-container">
        <div class="table-responsive">
            <table class="table">
                <thead>
                    <tr>
                        <th>Mã môn học</th>
                        <th>Tên môn học</th>
                        <th>Lịch học</th>
                        <th>Số lượng tối đa</th>
                        <th>Số lượng đã đăng ký</th>
                        <th>Hành động</th>
                        <th>Danh sách đăng ký</th> <!-- Cột mới -->
                        <th>Danh sách chờ</th>
                    </tr>
                </thead>
                <tbody>
                    <% courses.forEach(course=> { %>
                        <tr>
                            <td>
                                <%= course.courseCode %>
                            </td>
                            <td>
                                <%= course.courseName %>
                            </td>
                            <td>
                                <%= course.schedule %>
                            </td>
                            <td>
                                <%= course.capacity %>
                            </td>
                            <td>
                                <%= course.enrolled %>
                            </td>
                            <td>
                                <a href="/courses/register/<%= course._id %>" class="btn-action btn-register">
                                    <i class="fas fa-check me-2"></i>Đăng ký
                                </a>
                            </td>
                            <td>
                                <a href="/courses/enrolled/<%= course._id %>" class="btn-action btn-enrolled">
                                    <i class="fas fa-eye me-2"></i>Xem
                                </a>
                            </td>
                            <td>
                                <a href="/courses/waiting/<%= course._id %>" class="btn-action btn-waiting">
                                    <i class="fas fa-eye me-2"></i>Xem
                                </a>
                            </td>
                        </tr>
                        <% }) %>
                </tbody>
            </table>
        </div>
    </div>

    <!-- Nút Quay về trang chủ -->
    <div class="button-container">
        <a href="/" class="btn-action btn-home">
            <i class="fas fa-home me-2"></i>Quay về trang chủ
        </a>
    </div>

    <!-- Footer -->
    <footer>
        <p>© 2025 Quản lý khóa học. Mọi quyền được bảo lưu.</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>