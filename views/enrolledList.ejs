<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Danh sách đăng ký - <%= course.courseName %>
    </title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        h1 {
            text-align: center;
            color: #333;
            margin-bottom: 30px;
            font-weight: bold;
            font-size: 2rem;
        }

        /* Tùy chỉnh bảng */
        .table-container {
            max-width: 800px;
            margin: 0 auto;
            background-color: #fff;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            overflow: hidden;
        }

        .table {
            margin-bottom: 0;
        }

        .table th {
            background-color: #4CAF50;
            color: white;
            font-weight: bold;
            text-align: center;
            padding: 15px;
            border: none;
        }

        .table td {
            text-align: center;
            padding: 15px;
            vertical-align: middle;
            border: 1px solid #ddd;
        }

        .table tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        .table tr:hover {
            background-color: #f1f1f1;
            transition: background-color 0.3s ease;
        }

        /* Tùy chỉnh liên kết Quay lại */
        .btn-back {
            display: inline-flex;
            align-items: center;
            margin-top: 20px;
            text-decoration: none;
            color: #fff;
            background-color: #007BFF;
            padding: 8px 16px;
            border-radius: 25px;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

        .btn-back:hover {
            background-color: #0056b3;
            transform: translateY(-2px);
        }

        /* Tùy chỉnh thông báo không có dữ liệu */
        .no-data {
            text-align: center;
            color: #555;
            margin-top: 20px;
            font-size: 1.1rem;
            background-color: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            max-width: 800px;
            width: 100%;
        }

        /* Đảm bảo responsive */
        @media (max-width: 768px) {
            h1 {
                font-size: 1.5rem;
            }

            .table-container {
                margin: 10px;
            }

            .table th,
            .table td {
                padding: 10px;
                font-size: 0.85rem;
            }

            .btn-back {
                font-size: 0.9rem;
                padding: 8px 12px;
            }

            .no-data {
                font-size: 1rem;
                padding: 15px;
                margin: 10px;
            }
        }

        /* Footer */
        footer {
            background: linear-gradient(90deg, #007bff, #00c4cc);
            color: white;
            text-align: center;
            padding: 1rem 0;
            margin-top: auto;
            width: 100%;
        }

        footer p {
            margin: 0;
            font-size: 0.9rem;
        }
    </style>
</head>

<body>
    <!-- Tiêu đề -->
    <h1><i class="fas fa-users me-2"></i>Danh sách đăng ký - <%= course.courseName %>
    </h1>

    <!-- Bảng danh sách đăng ký -->
    <% if (enrolledList.length> 0) { %>
        <div class="table-container">
            <div class="table-responsive">
                <table class="table">
                    <thead>
                        <tr>
                            <th>Mã sinh viên</th>
                            <th>Tên sinh viên</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% enrolledList.forEach(student=> { %>
                            <tr>
                                <td>
                                    <%= student.studentId %>
                                </td>
                                <td>
                                    <%= student.name %>
                                </td>
                            </tr>
                            <% }) %>
                    </tbody>
                </table>
            </div>
        </div>
        <% } else { %>
            <p class="no-data"><i class="fas fa-info-circle me-2"></i>Hiện tại không có sinh viên nào trong danh sách
                đăng ký.</p>
            <% } %>

                <!-- Liên kết Quay lại -->
                <a href="/courses" class="btn-back">
                    <i class="fas fa-arrow-left me-2"></i>Quay lại danh sách môn học
                </a>

                <!-- Footer -->
                <footer>
                    <p>© 2025 Quản lý khóa học. Mọi quyền được bảo lưu.</p>
                </footer>

                <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>