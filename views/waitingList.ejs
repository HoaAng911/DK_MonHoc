<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Danh sách chờ - <%= course.courseName %>
    </title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
        }

        h1 {
            text-align: center;
            color: #333;
            margin-bottom: 20px;
        }

        table {
            width: 100%;
            max-width: 800px;
            margin: 0 auto;
            border-collapse: collapse;
            background-color: #fff;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        th,
        td {
            padding: 12px 15px;
            text-align: center;
            border: 1px solid #ddd;
        }

        th {
            background-color: #4CAF50;
            color: white;
            font-weight: bold;
        }

        tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        tr:hover {
            background-color: #f1f1f1;
        }

        a {
            display: inline-block;
            margin-top: 20px;
            text-decoration: none;
            color: #fff;
            background-color: #007BFF;
            padding: 8px 16px;
            border-radius: 4px;
            transition: background-color 0.3s;
        }

        a:hover {
            background-color: #0056b3;
        }

        .no-data {
            text-align: center;
            color: #555;
            margin-top: 20px;
            font-size: 16px;
        }
    </style>
</head>

<body>
    <h1>Danh sách chờ - <%= course.courseName %>
    </h1>
    <% if (course.waitingList.length> 0) { %>
        <table border="1">
            <tr>
                <th>Mã sinh viên</th>
                <th>Tên sinh viên</th>
            </tr>
            <% course.waitingList.forEach(student=> { %>
                <tr>
                    <td>
                        <%= student.studentId %>
                    </td>
                    <td>
                        <%= student.name %>
                    </td>
                </tr>
                <% }) %>
        </table>
        <% } else { %>
            <p class="no-data">Hiện tại không có sinh viên nào trong danh sách chờ.</p>
            <% } %>
                <a href="/courses">Quay lại danh sách môn học</a>
</body>

</html>